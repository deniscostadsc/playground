http://www.urionlinejudge.com.br/judge/problems/view/1074

Read an integer value N. After, read these N values and print
a message for each value saying if this value is odd, even,
positive, negative or null.

The message must be in uppercase letters.

Input

The first line of input is an integer N (N < 10000), that
indicates the total number of test cases. Each case is a
integer number X (-107 < X <107)..

Output

For each test case, print a corresponding message, according with the below example.
