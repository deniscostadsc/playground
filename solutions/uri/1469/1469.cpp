#include <cstdint>
#include <iostream>

struct person {
    int16_t age;
};

int main() {
    int16_t n, m, i, age, x, y;

    std::cin >> n >> m >> i;

    while (n--) {
        std::cin >> age;
    }

    while (m--) {
        std::cin >> x >> y;
    }

    while (i--) {
    }

    return 0;
}
