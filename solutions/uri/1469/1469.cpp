#include <iostream>

struct person {
    int age;
};

int main() {
    int n, m, i, age, x, y;

    std::cin >> n >> m >> i;

    while (n--) {
        std::cin >> age;
    }

    while (m--) {
        std::cin >> x >> y;
    }

    while (i--) {
    }
    return 0;
}
