---
name: Test
on:
  push:
    paths:
      - '.github/**'
      - 'Makefile'
      - 'scripts/**'
      - 'tests/**'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run tests
        run: make test
